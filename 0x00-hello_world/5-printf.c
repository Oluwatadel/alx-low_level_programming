#include <stdio.h>
/**
 * main -Entry point
 *
 * display a given string with printf
 *
 * Return: always 0
 *
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
