#include <stdio.h>
#include "main.h"
/**
 * main - writes the character c to stdout
 * Description: To printed out because of the _putchar function
 * Return: On success 0.
 */
int main(void)
{
	putchar();

	return (0);
}
