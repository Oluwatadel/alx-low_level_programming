#include "main.h"

/**
 * _islower - To check is c is lower case
 *
 * @c: To check if c is lower case and return eith 1 or zero
 *
 * Return: On success 0.
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')

	{
		return (1);

	} else

	{

		return (0);
	}

}
